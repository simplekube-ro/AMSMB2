## 1. Seam protocol and test doubles (design D1, D6)

- [x] 1.1 Red: rewrite `AMSMB2Tests/SMBTransportTests.swift`'s mock scenarios against the push shape — connect with an `InboundRecorder` handler, `deliver` two chunks (in order), `signalGracefulEOF()` once (nothing after), `signalError(_:)` once with a `POSIXError` (nothing after), `send(_:)` observed through `sentChunks()`, a failing connect never invokes the handler, and `deliver` after `close()` is not delivered; add `InboundRecorder` (final class, `NSLock`, `[Result<Data, POSIXError>]`, `waitForDeliveries(count:timeout:)`) to `AMSMB2Tests/TestUtilities.swift`; verify the file fails to compile because `connect(host:port:onReceive:)`, `deliver`, `signalError` and `sentChunks` do not exist
- [x] 1.2 Green: change `SMBTransport` in `AMSMB2/SMBTransport.swift` to `connect(host:port:onReceive:)` / `send(_:)` / `close()` with `onReceive: @escaping @Sendable (Result<Data, POSIXError>) -> Void` and the delivery contract in the doc comment (serial, in order, EOF and error terminal, nothing once close has begun, a throwing connect never invokes the handler, a rejected repeat connect never replaces it, handler must not suspend); rewrite `MockTransport` (still an actor: stores the handler on connect, `deliver`/`signalGracefulEOF`/`signalError` invoke it terminal-once and never after `close()`, `send` appends to a sent log with `sentChunks()` and `waitForSent(count:)`, loopback and `sendsAreDropped` deleted); add the parameter to `GatedOutcomeTransport` and `GatedConnectTransport`; drop the `sendsAreDropped:` argument in `AMSMB2Tests/SMB2DisconnectReclaimTests.swift`; verify `swift test --disable-sandbox --filter SMBTransportTests` passes
- [x] 1.3 Dead-code sweep for this group: `grep -rn "sendsAreDropped\|func receive()" AMSMB2 AMSMB2Tests` returns nothing outside the not-yet-converted transport files listed in groups 3–4

## 2. Bridge: delete the inbound pump (design D2, D5)

- [x] 2.1 Red: in `AMSMB2Tests/TransportBridgeTests.swift` replace the pump-based inbound tests with push-based ones on the same scenarios — bytes injected through the mock are drained by `cRecv`; would-block when empty and open; EOF → 0 after drain; cross-chunk gather; partial drain advances the cursor across a boundary; non-zero-`startIndex` slice; the inbound-ready signal fires inside the delivery (assert the signal count and store contents synchronously after `await mock.deliver(...)` returns, no expectation wait); a delivery (and separately an EOF, and an error) made before `setInboundReadyHandler` causes exactly one signal at registration and is then drained by `cRecv` (assert the signal count, not only the bytes — this is the lost-wakeup test design D2 requires); registration with an empty open store fires no signal; an outbound `send` failure on the mock is reported by `cRecv` as the error after any buffered bytes (precedence unchanged); delivery after `close()` appends nothing and fires no signal; the bridge is deallocated while the mock still holds the handler (`weak var` probe); outbound tests observe `mock.sentChunks()`; the double-start test covers only `startOutboundPump()`; verify the file fails to compile (`startInboundPump`/`startPumps` still exist and `bridge.connect` does not pass a handler)
- [x] 2.2 Green: in `AMSMB2/TransportBridge.swift` delete `inboundPumpTask`, `startInboundPump()`, `startPumps()`, `inboundPump()`, `appendInbound`, `setInboundEOF`, `setInboundError`; add `deliverInbound(_ result: Result<Data, POSIXError>)` and the private `applyInbound` it and the outbound pump's send-failure path share (under the lock: return if `isClosed`; non-empty bytes → `InboundSignposts.chunk` + append + count; empty → `inboundEOF = true`; error → `inboundError`; no terminal-once flag; then fire `_onInboundReady` outside the lock); make `setInboundReadyHandler` store the handler under the lock, read whether the store already holds bytes/EOF/error, and invoke the handler once outside the lock if so; make `connect(host:port:)` call `transport.connect(host:port:) { [weak self] in self?.deliverInbound($0) }`; `close()` cancels only the outbound task; update the class doc comment and the `_onInboundReady` comment; in `AMSMB2/Context.swift` replace `bridge.startPumps()` with `bridge.startOutboundPump()`, add the load-bearing-ordering comment at that call (outbound pump after the inbound-ready registration, design D2) and fix the `teardownSeam` doc comment ("cancels the pump Tasks" → the outbound pump task); verify `swift test --disable-sandbox --filter TransportBridgeTests` passes
- [x] 2.3 Servicing loop: update `AMSMB2Tests/SMB2ServicingLoopTests.swift` — `testInboundReadyCallbackFiresWhenBytesAreAppended` connects the bridge to the mock and injects via `mock.deliver`; drop `sendsAreDropped: true` from `testConnectWithBridgeCompletesWithoutHang` (nothing loops back any more) and from every other call site; verify `swift test --disable-sandbox --filter SMB2ServicingLoopTests` passes and the debounce/teardown tests are unmodified

## 3. TCPTransportApple forwards (design D3, D4)

- [x] 3.1 Red: in `AMSMB2Tests/TCPTransportAppleTests.swift` pass an `InboundRecorder` to every `connect` call; convert the receive-cancellation and the direct `InboundBufferingHandler` onCancel tests into: bytes written by the local listener arrive at the recorder in order (two writes → two deliveries, no coalescing by the transport), listener-side close → exactly one empty-`Data` delivery and nothing after, `close()` on a live channel → no delivery for the teardown and the handler closure released afterwards (`weak var` probe on an object the closure captures), error → one failure that is a `POSIXError`, a zero-length read driven directly into the forwarding handler (NIO `EmbeddedChannel` or a direct `channelRead` call with an empty `ByteBuffer`) → no delivery and a later non-empty read delivered, and a second `connect` on an established transport (rejected with `EISCONN`) → the first recorder still receives the next write; verify the file fails to compile
- [x] 3.2 Green: in `AMSMB2/TCPTransportApple.swift` replace `InboundBufferingHandler` with `InboundForwardingHandler` (lock-guarded handler closure + `terminated` flag; `channelRead` returns early on `readableBytes == 0` before the `TransportRead` signpost, otherwise keeps the `Data` copy and the signpost then forwards `.success`; `channelInactive` forwards `.success(Data())` once; `errorCaught` forwards `.failure(Self.mapError(error))` once then closes the channel; `signalClosed()` sets `terminated` and nils the closure); `connect(host:port:onReceive:)` installs the closure into the handler immediately after the one-shot reservation succeeds and before the bootstrap runs (a rejected call never touches it; a throwing connect never invokes it); the `open → closing` transition in `close()` calls `signalClosed()` first (delivery stops before the channel is closed) and the closure is nil once `.closed` is published; delete `receive()`; verify `swift test --disable-sandbox --filter TCPTransportAppleTests` passes, including the unchanged one-shot-connect and close-lifecycle tests

## 4. QUICTransportApple forwards (design D3, D4)

- [x] 4.1 Red: in `AMSMB2Tests/QUICTransportAppleTests.swift` give the helpers that connect (`connectedTransport`, `launchConnect`) an `InboundRecorder` parameter (default a fresh one) and pass it through `connect` (`makeTransport` / `makeGatedTransport` only construct and take none); convert the eight `receive()`-parking tests to recorder assertions on the same scenarios (peer EOF → one empty delivery; chunks before any drain → delivered in order; local close then `.cancelled` → no delivery; unsolicited `.failed` → one `POSIXError` failure; unsolicited `.cancelled` → one failure; `.cancelled` racing close → at most one delivery, recorded local close never overwritten; receive-side driver error → one failure; peer EOF followed by `.cancelled` and separately by `.failed` → exactly one delivery total; a rejected second `connect` with a different recorder → bytes still reach the first recorder; nothing after a terminal delivery); replace the never-connected-`receive()`, receive-after-close and close-with-parked-receiver tests with "close makes no inbound delivery and releases the closure" and "a never-connected transport holds no handler"; verify the file fails to compile
- [x] 4.2 Green: in `AMSMB2/QUICTransportApple.swift` store the `onReceive` closure under the lock in `connect` only after the one-shot reservation succeeds; add one lock-guarded `deliveryTerminated` flag checked and set by all four producers (EOF branch, chunk-path failure, post-ready `.failed`, post-ready `.cancelled`) and by the `open → closing` transition; make `handleReceive` forward (`.success(empty)` → terminal EOF delivery, `.success(bytes)` → delivery, `.failure` → `lifecycle = .failed` + terminal failure delivery), route the post-ready `abnormalLoss` path to a `.failure(POSIXError(.ECONNRESET))` delivery, and nil the closure when `.closed` is published; delete `receive()`, `inboundChunks`, `receiveWaiter`, `receiveError`, `inboundEOF`; update the `transport.connect(host:port:)` call in `AMSMB2/SMBQUICCertificateProbe.swift` (line ~159) to pass a no-op handler; verify `swift test --disable-sandbox --filter 'QUICTransportAppleTests|QUICSeamConnectTests|QUICTrustTests|SMBQUICCertificateProbeTests'` passes
- [x] 4.3 Full unit suite and platform gate: `swift test --disable-sandbox` green with no new skips; `make linuxtest` — the Linux target compiles `SMBTransport.swift` and `MockTransport.swift` (unguarded), so this is the strict-concurrency gate for the `Result<Data, POSIXError>` payload crossing the actor boundary (design D1); `grep -rn "receive()\|inboundPump\|InboundBufferingHandler\|startPumps\|sendsAreDropped" AMSMB2 AMSMB2Tests` returns nothing (docs are checked in 5.1, where the migration section legitimately names the removed `receive()`); every new symbol (`deliverInbound`, `InboundForwardingHandler`, `InboundRecorder`, `deliver`, `signalError`, `sentChunks`, `waitForSent`) has at least one call site outside its definition file

## 5. Documentation (design D7, D8)

- [x] 5.1 `docs/API.md`: replace the `SMBTransport` listing and the EOF-convention bullet with the push protocol and delivery contract, and add a "Migrating a conformer from `receive()`" snippet (a `receive()` loop becomes: store the handler at connect, call it from the network callback, terminal-once, stop at close); `docs/ARCHITECTURE.md`: remove the inbound pump node from the transport diagram and rewrite the "Inbound." paragraph and the `SMBTransport` row so the Transport Layer section covers the pushed inbound path and the outbound pump task (the `architecture-docs` delta); main-spec Purposes edited directly: `openspec/specs/transport-bridge/spec.md` ("tear down both pump tasks" → the outbound pump task), `openspec/specs/transport-seam/spec.md` ("connect/send/receive/close" → connect-with-receiver/send/close; "loopback double" → push double) and `openspec/specs/tcp-transport-apple/spec.md` ("connect/send/receive/close with cancellation and incremental inbound buffering" → connect/send/close with cancellation and pushed inbound delivery); verify `grep -n "receive()" docs/*.md` matches only the migration section and the "no `receive()`" bullet (text describing the removed API), and the diagram still renders (mermaid block parses)
- [x] 5.2 `docs/PROFILING.md`: in Metrics, rewrite the intro sentence ("The inbound path is a pull loop with two executor hops per burst" → one hop after this change) and the `Coalescing ratio` / `Pump-hop latency` rows per design D7 (1.00 by construction because zero-length reads are skipped before `TransportRead`; the row is the residual in-callback hand-off; the #45 delta is read from per-thread shares, dispatch/pass percentiles, active throughput and stalls); rewrite "Using the baseline" step 3 to name that evidence instead of pump-hop latency and the coalescing ratio; add an empty "After (inbound push-conversion)" section with the same field table and a delta table skeleton; `AMSMB2/Signposts.swift`: correct the type doc comment ("two executor hops of the inbound pull loop", "the `TransportRead → InboundChunk` gap is the cooperative-pool pump hop") and the `chunk(bytes:)` comment ("when the pump is behind …"); verify `swift test --disable-sandbox --filter SignpostContractTests` still passes (the identifier block is untouched) and `grep -n "pump" AMSMB2/Signposts.swift` is empty

## 6. Review and integration gates

- [x] 6.1 Sub-agent pipeline per CLAUDE.md: `swift-code-reviewer` reviews the diff (lock discipline in the forwarding handlers — closure read under the lock, invoked outside it; terminal-once flags; weak capture; no `await` under a lock; signpost placement unchanged), `swift-platform-developer` addresses findings, reviewer verifies; repeat until the reviewer has no open findings; verify the final review states no findings remain
- [x] 6.2 `make integrationtest` against the Docker Samba server: `SMB2SeamIntegrationTests` (connect/NTLM, list, large read/write, cancel/timeout) and the rest of the integration suite pass through the NIO TCP transport with no unlisted failures; verify the summary shows zero failures outside `scripts/known-flaky.txt`

## 7. Before/after gate (operator, design D7) — needs the Apple TV attached

- [x] 7.1 After 1–6 merge, tag `6.0.0-rc5` with release notes that lead with the **BREAKING** `SMBTransport` change (`receive()` removed; `connect(host:port:onReceive:)`; delivery contract) and the migration snippet from `docs/API.md`; re-pin RandomPlayer to rc5 (record the commit) and build with Product ▸ Profile on the Apple TV; verify the process list export shows no debugger-injected library and the instrument set is Time Profiler + `os_signpost`
- [x] 7.2 Capture three runs per `docs/PROFILING.md` (uncached videos of size comparable to the baseline's, same device) and run `scripts/profile-summary.sh` on each; verify all five signpost names appear, `InboundChunk` bytes equal `TransportRead` bytes (allowing at most one trailing `TransportRead` that raced bridge teardown at the end of the recording), the coalescing ratio is 1.00 with `reads per chunk: 1 read` only, and no pairing errors other than that trailing read
- [x] 7.3 Fill the After section with the median run (by `ServicePass` median) and the delta table against the Baseline (per-thread shares with the cooperative-pool threads called out, `ServiceDispatch` and `ServicePass` percentiles, active throughput, stalls, residual pump-hop row); the merge condition is no regression in dispatch/pass percentiles or active throughput and no stalls; commit; verify the numbers match the script output of the archived run
- [x] 7.4 Comment on issue #45 with the delta and the doc link, note in #46 that its re-profile must now be taken against the rc5 After numbers; verify both issues show the cross-reference

## 8. Verification gate

- [x] 8.1 Run `/opsx:verify` and confirm every spec scenario maps to a test, the integration suite, or the operator gate (7.x), and that proposal/design/specs describe what shipped; then `/opsx:archive`
