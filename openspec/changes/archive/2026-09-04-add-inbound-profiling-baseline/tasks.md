## 1. Identifier contract and signpost helper (design D1, D2, D5)

- [x] 1.1 Red: add `AMSMB2Tests/SignpostContractTests.swift` (under `#if canImport(Network)`) that locates `docs/PROFILING.md` from `#filePath`, parses the `subsystem:` / `category:` lines of its instrument-filter block, and asserts they equal `InboundSignposts.subsystem` / `.category`; verify it fails to compile (no helper) and, once the helper exists, fails because the doc is missing
- [x] 1.2 Green: add `AMSMB2/Signposts.swift` (MIT file header; `#if canImport(Network)`) with `InboundSignposts` — `subsystem = "ro.SimpleKube.AMSMB2"`, `category = "Inbound"`, the shared `OSLog`, and the emit functions `transportRead(bytes:)`, `chunk(bytes:)`, `recv(bytes:)`, `recvWouldBlock()`, `dispatchBegin(for:)`, `dispatchEnd(for:)`, `passBegin(for:)`, `passEnd(for:terminal:)`, each starting with `guard log.signpostsEnabled else { return }` and using `os_signpost` with `%d` metadata only, IDs derived via `OSSignpostID(log:object:)`; the doc comment states the reentrancy property (D4); verify `swift build --disable-sandbox` succeeds and every function has the guard (`grep -c signpostsEnabled AMSMB2/Signposts.swift` equals the function count)
- [x] 1.3 Write the skeleton of `docs/PROFILING.md` containing the instrument-filter block the test parses; verify `swift test --disable-sandbox --filter SignpostContractTests` passes

## 2. Instrument the hot path (design D3, D4, D5)

- [x] 2.1 Red: add the pairing test to `AMSMB2Tests/SMB2ServicingLoopTests.swift` — coalesce (three `consumeInboundReadySignal()` → true/false/false, `beginServicePass()` → true, again → false), re-arm (`beginServicePass()`, `consumeInboundReadySignal()` → true, `beginServicePass()` → true), teardown (`consumeInboundReadySignal()` → true, `teardownSeam()`, `beginServicePass()` → false); verify it fails to compile because `beginServicePass()` returns `Void`
- [x] 2.2 Green: in `SMB2Client` add `clearInboundReadySignal() -> Bool` (clears under `serviceFlagLock`, returns whether it was armed, emits `dispatchEnd(for: self)` only then), make `beginServicePass()` `@discardableResult -> Bool` via it, route `teardownSeam`'s reset through it, emit `dispatchBegin(for: self)` on the `false → true` flip in `consumeInboundReadySignal`, and bracket `serviceContextForSeam` with `passBegin` after the context guard and `defer { passEnd(for: self, terminal: transportBridge == nil) }` (covers both the direct service-failure teardown and the one inside `flushOutboundForSeam`); verify `swift test --disable-sandbox --filter SMB2ServicingLoopTests` passes with the new test and the existing debounce/teardown tests unmodified
- [x] 2.3 `TransportBridge.appendInbound`: emit `chunk(bytes:)` inside the non-empty branch; `cRecv`: emit `recv(bytes: copied)` before the bytes return, `recv(bytes: 0)` before the EOF return, `recvWouldBlock()` before the `EAGAIN` return; `InboundBufferingHandler.channelRead` in `TCPTransportApple.swift`: emit `transportRead(bytes:)` after the `Data` copy; verify `swift test --disable-sandbox --filter 'TransportBridgeTests|TCPTransportAppleTests'` passes unmodified (gather, cursor advance, slice copy, return precedence, transport receive)
- [x] 2.4 Dead-code gate and platform check: confirm each `InboundSignposts` function has exactly one call site outside `Signposts.swift` (`grep -n "InboundSignposts\." AMSMB2/*.swift`); verify the full suite `swift test --disable-sandbox` is green with no new skips and the Linux test target compiles and passes (`make linuxtest`, i.e. `swift test`, not just `swift build`)

## 3. Trace summary script (design D6)

- [x] 3.1 Red: add `test-fixtures/profiling/sample-export/{time-profile.xml,os-signpost.xml}` — a small hand-written `xctrace` export (with `ref`/`id` back-references) covering three threads and all five signpost names with known totals, including one coalesced group (three `TransportRead`s summing to one `InboundChunk`) and one `terminal=1` `ServicePass`, plus `expected.txt` holding the exact output the script must produce; verify `diff <(scripts/profile-summary.sh test-fixtures/profiling/sample-export) test-fixtures/profiling/sample-export/expected.txt` fails (script absent)
- [x] 3.2 Green: add `scripts/profile-summary.sh <bundle.trace | export-dir>` (bash + inline python3): export the `time-profile` and `os-signpost` tables via `xcrun xctrace export --xpath` when given a bundle, skip the export when given a directory with the two XML files, resolve back-references, print total samples and the per-thread table, per-signpost count / byte totals / size percentiles (`TransportRead`, `InboundChunk`, `RecvDrain`), duration percentiles (`ServiceDispatch`, `ServicePass` with terminal passes listed separately and excluded), the coalescing ratio, pump-hop latency by FIFO byte-sum pairing (zero-byte reads skipped; overshoot reported as a pairing error; `n/a` for ratio and latency when there are no `TransportRead` events), bytes still buffered at the end, and derived throughput, filtered to the subsystem; verify the `diff` from 3.1 is empty, `shellcheck` is clean and `bash -n` parses
- [x] 3.3 Edge paths: verify a nonexistent path and a directory without `time-profile.xml` each exit non-zero with a one-line message and no traceback; verify a copy of the fixture with `os-signpost.xml` removed prints the per-thread table plus the "no ro.SimpleKube.AMSMB2 signposts" line and exits 0; verify a copy with the `TransportRead` rows removed prints `n/a` for the coalescing ratio and pump-hop latency with no pairing errors and exits 0; verify running the script twice on the fixture gives identical output
- [x] 3.4 Operator-local export check (not required by the spec): run the script on `../RandomPlayer/ios.trace` and verify the export step works against a real Xcode 26.6 bundle (per-thread table, "no signposts" line, exit 0); record the `xctrace version` used in `docs/PROFILING.md`

## 4. Procedure document (design D7)

- [x] 4.1 Complete `docs/PROFILING.md`: Purpose, Prerequisites, Build (Profile action; the `libBacktraceRecording.dylib` / `libLogRedirect.dylib` disqualifier and the `xctrace export --xpath '/trace-toc/run[1]/processes'` check), Instruments (GUI steps and the `xcrun xctrace record --device … --template 'Time Profiler' --instrument os_signpost --time-limit … --launch` form), Workload (named video, 120 s playback, five fixed seeks), Export and summarise, Metrics definitions (the two hops named separately; the coalescing ratio; `TransportRead` marked TCP-only; terminal passes; the teardown-closed dispatch interval note), an empty Baseline table, and the before/after rule for #45/#46 (same device, same video, three runs, median); verify every command in the doc was executed at least once in this session or is marked "device-only"
- [x] 4.2 Link the doc from `README.md`'s Documentation section and add the `Signposts.swift` row (layer: Transport (Apple)) to the file map in `docs/ARCHITECTURE.md`; verify both links resolve and `SignpostContractTests` still passes

## 5. Baseline capture (operator gate, design D7, D8) — needs the Apple TV attached

- [x] 5.1 Tag `6.0.0-rc4` after 1–4 merge, with release notes stating the library now emits signposts under `ro.SimpleKube.AMSMB2` / `Inbound` and linking `docs/PROFILING.md`; re-pin RandomPlayer to it and build with Product ▸ Profile on the Apple TV; verify the process list export shows no debugger-injected library and the instrument set is Time Profiler + `os_signpost`
- [x] 5.2 Run the workload three times per the doc and run `scripts/profile-summary.sh` on each bundle; verify all five signpost names appear with non-zero counts, `InboundChunk` bytes equal `TransportRead` bytes, `RecvDrain` bytes do not exceed `InboundChunk` bytes, the coalescing ratio and pump-hop latency columns are populated with no pairing errors, and the per-thread table shows the cooperative-pool threads
- [x] 5.3 Fill the Baseline section with the median run (date, device, tvOS, AMSMB2 version, `xctrace version`, script output, stalls observed) and commit; verify the numbers in the doc match the script output of the archived run
- [x] 5.4 Comment on issue #44 with the summary and the doc link; add a "gated on #44 baseline" comment to #45 and #46 referencing `docs/PROFILING.md`; verify all three issues show the cross-references

## 6. Verification gate

- [x] 6.1 Run `/opsx:verify` and confirm every spec scenario maps to a test, a script run on the fixture, or the operator gate (5.x) and that the artifacts describe what shipped; then `/opsx:archive`
