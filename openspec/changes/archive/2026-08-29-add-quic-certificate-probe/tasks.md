## 1. Shared validation and capture primitives (design D2, D3, D4)

- [x] 1.1 Red: add `QUICSeamConnectTests` cases calling `SMB2Client.validatedQUICEndpoint(_:)` directly (numeric host → `EINVAL`, port `0`/`65536`/oversized → `EINVAL`, default 443, explicit port honored, bracketed IPv6 form still rejected as numeric) and `validateQUICEndpoint(host:port:)` on a pre-parsed pair; verify they fail to compile before the helpers exist
- [x] 1.2 Green: add `validateQUICEndpoint(host:port:)` (numeric-host + 1...65535) and `validatedQUICEndpoint(_ server:)` (parse with the `.quic` default port, then validate) inside `Context.swift`'s `#if canImport(Network)` region; `connect`'s `.quic` branch calls `validateQUICEndpoint` on its already-parsed endpoint so `parseSeamEndpoint` is still invoked exactly once (keep the comment true); verify `swift test --disable-sandbox --filter QUICSeamConnectTests` passes and the existing connect-side tests are unchanged
- [x] 1.3 Red: add a unit test for `NWConnectionQUICDriver.certificateChainDER(from:)` building a `SecTrust` with `SecTrustCreateWithCertificates` from `openssl`-generated certificates (reuse `QUICTrustTests.selfSignedDER` / `serverTrust`, skipping when `openssl` is unavailable) and asserting the returned `[Data]` is leaf first and byte-equal to the generated DER, plus a case proving an empty/`nil` chain yields `[]`; verify it fails before the helper exists
- [x] 1.4 Green: implement `certificateChainDER(from:)` (`SecTrustCopyCertificateChain` → `SecCertificateCopyData`, `nil` → `[]`) and the internal `final class QUICCertificateCaptureSlot: @unchecked Sendable` (NSLock-guarded `[Data]?`, `store`/`chain`); verify the 1.3 test passes and a slot unit test proves a store on one queue is visible from another
- [x] 1.5 Add `QUICResolvedTrust.capture(QUICCertificateCaptureSlot)` and the `NWConnectionQUICDriver.init` verify-block branch (capture → `slot.store` → `complete(false)`; `store` itself ignores an empty chain, so nothing is captured in that case); this production wiring is not unit-testable (`sec_trust_t` is not constructible) — verify by code inspection (the branch calls `complete(false)` on every path, exactly once) and rely on the interop gate (§3); verify `swift build --disable-sandbox` succeeds and `QUICTrustTests` still prove `.system`/`.customRoots`/`.insecure` resolve unchanged after the exhaustive switch is edited

## 2. Probe entry point (design D1, D5, D6)

- [x] 2.1 Red: create `AMSMB2Tests/SMBQUICCertificateProbeTests.swift` with scripted-driver tests through the internal entry point: (a) slot pre-filled + `.waiting(EPROTO, .fatal)` → returns the chain; (b) empty slot + fatal wait → throws `EPROTO` with `NSUnderlyingErrorKey`; (c) slot pre-filled + `.ready` → returns the chain; (d) empty slot + `.ready` → throws `EPROTO`; (e) deadline fired with empty slot → `ETIMEDOUT`; (f) deadline fired with filled slot → returns chain; (g) task cancelled while waiting, with filled slot → `CancellationError`; (h) task cancelled before the attempt is committed → `CancellationError` with `driver.didStart == false` and `cancelCount == 0`; cases (a)–(g) assert `driver.cancelCount == 1` after return, all cases assert the deadline scheduler was cancelled and that the slot is read only after `close()` returned (a scripted driver that stores into the slot from its `cancel()` must still be observed); verify they fail to compile before the type exists
- [x] 2.2 Green: create `AMSMB2/SMBQUICCertificateProbe.swift` — public `@available`-gated caseless enum with `fetchServerCertificateChain(server:timeout:)`, the internal `(server:timeout:driverFactory:deadline:)` entry point implementing the D1 outcome table (always `await transport.close()`, slot read after close; configuration built as `SMBQUICConfiguration(trustPolicy: .system, connectTimeout: connectTimeout)` from the once-normalized value, which is also passed as the internal initializer's `connectTimeout:`), the Linux `#else` body throwing `ENOTSUP` (Glibc constant, same spelling as `SMB2Manager.connectShare`); the doc comment spells out the `host[:port]` grammar and the UDP/443 default; verify `swift test --disable-sandbox --filter SMBQUICCertificateProbeTests` passes
- [x] 2.3 Red/Green: add probe-level validation tests (numeric host, bad port, invalid timeout → `EINVAL`, and that the driver factory is never invoked in those cases; `timeout` above 3600 clamps); verify they pass
- [x] 2.4 Verify `swift build --disable-sandbox` and the full `swift test --disable-sandbox` pass on macOS, and `make linuxtest` (or the Docker Linux build) compiles the new file with the `ENOTSUP` body and no `Network`/`Security` imports leaking

## 3. Interop gate (spec: interop-verified scenarios)

- [x] 3.1 Add `SMB2QUICInteropTests.testProbeReturnsServerLeaf`: probe `SMB_QUIC_SERVER`; assert the chain is non-empty and `chain.first` parses as a certificate; when the new optional env var `SMB_QUIC_LEAF_DER` (DER path of the expected server leaf) is set, additionally assert `chain.first`'s SHA-256 equals its SHA-256 and — when it is byte-equal to `SMB_QUIC_CA_DER` (self-signed target) — that the chain has exactly one element; document `SMB_QUIC_LEAF_DER` in the interop file's env header; verify it passes against `win2k22.kaveman.intra` (leaf == CA) and against the Samba/private-CA rig (leaf ≠ CA)
- [x] 3.2 Add `testProbedChainConnectsAsCustomRoots`: feed the probe's leaf into `.customRoots([leaf])` and complete a `.quic` `connectShare` + directory listing; verify it passes against the Windows target and (leaf + intermediates if any) against the Samba/private-CA rig
- [x] 3.3 Add `testProbeAgainstTCPOnlyPortDoesNotHang`: probe `host:445` with `timeout: 3` and assert `ETIMEDOUT` or `EPROTO` within `timeout + 2 s`; verify it passes live
- [x] 3.4 Record the probe run (elapsed time, DER count, SHA-256 match, error codes for the negative case, and the publicly-trusted-server chain observation if performed) in `docs/INTEROP-QUIC.md` § Windows Server 2022; verify the section is present

## 4. Documentation (api-reference delta)

- [x] 4.1 `docs/API.md`: add `SMBQUICCertificateProbe` to the public-types table and to the Swift-only/Objective-C note, a `## SMBQUICCertificateProbe` section (signature, `host[:port]` grammar, capture-only contract, `server`/`timeout` validation, that `timeout` is independent of `SMBQUICConfiguration.connectTimeout` and why, outcome table incl. compile-time availability and Linux `ENOTSUP`), update the error-code table rows for `ETIMEDOUT`/`EPROTO`/`ENOTSUP`/`EINVAL` per the `api-reference` delta, and a "Trust on first use" recipe in the SMB-over-QUIC section (probe → show subject/SAN/validity/SHA-256 → `.customRoots([leaf])`) carrying the first-contact caveat (confirm the fingerprint out of band — an on-path attacker can present their own chain; the anchor replaces the system roots for that connection); verify every scenario in the `api-reference` delta is satisfied by reading the rendered sections
- [x] 4.2 `README.md`: extend the SMB-over-QUIC snippet with the TOFU flow and a security note (the probe never trusts — the user does, after confirming the SHA-256 out of band; first contact is only as trustworthy as the network at that moment); verify the snippet compiles against the public API names
- [x] 4.3 `docs/ARCHITECTURE.md`: add `SMBQUICCertificateProbe` to the transport-layer type table and `SMBQUICCertificateProbe.swift` to the file structure map; note the capture-only resolved-trust mode in the QUIC conformer paragraph; verify the tables render

## 5. Verification gate

- [x] 5.1 Run `swift test --disable-sandbox` (all unit tests green, no skipped probe unit tests), the interop suite with `SMB_QUIC_SERVER` set, and `swiftformat --lint` / `swift-format lint`; confirm no new symbol lacks a non-test call site (`certificateChainDER`, `validatedQUICEndpoint`, `QUICCertificateCaptureSlot`, `.capture` all used in production code) — then run `/opsx:verify`
