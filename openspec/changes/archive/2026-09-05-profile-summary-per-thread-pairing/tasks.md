## 1. Fixtures first (design D4) — Red

- [x] 1.1 Add `test-fixtures/profiling/per-thread-export/os-signpost.xml`, hand-written in the style of `sample-export` (schema on the first `<node>`, rows split over two nodes, interned `id`/`ref` values, `uint64` metadata), with the D4 timeline: carry-over chunk on `0x101`; the interleaved group `0x101` read 1500 / `0x103` read 500 / `0x103` chunk 500 / `0x101` chunk 1500; a mismatch `0x101` read 800 → chunk 900; `0x103` reads 600 then 700 followed by one chunk of 700; a trailing read of 400 on `0x101`; minimal `RecvDrain` / `ServiceDispatch` / `ServicePass` rows on `0x102`; the header comment lists the timeline, the per-thread expected numbers and what the global FIFO reports for the same rows; copy `sample-export/time-profile.xml` alongside; verify `python3 -c 'import xml.etree.ElementTree as ET; ET.parse("test-fixtures/profiling/per-thread-export/os-signpost.xml")'` parses
- [x] 1.2 Write `test-fixtures/profiling/per-thread-export/expected.txt` by hand from the timeline (per-thread mode, D3 shape: `pairing: per-thread (2 delivery threads)`, no `reads per chunk` line, latency suffix with pairs / chunks without a read / reads without a chunk / pairing errors satisfying the D1 invariants against the fixture's counts, one `pairing error: mismatch: …` line naming the thread as `NIO-SGLTN-1-#1 (0x101)`); verify `diff <(scripts/profile-summary.sh test-fixtures/profiling/per-thread-export) test-fixtures/profiling/per-thread-export/expected.txt` fails against the unchanged script (it prints overshoot/underflow errors and no `pairing:` line)
- [x] 1.3 Add `test-fixtures/profiling/no-transport-read-export/` — `os-signpost.xml` is the per-thread fixture with every `TransportRead` row removed (header comment says so), `time-profile.xml` copied, `expected.txt` written by hand (`pairing:` line, `n/a` for the ratio and the latency, no error lines); verify its `diff` fails against the unchanged script (no `pairing:` line)
- [x] 1.4 Add the `pairing: global FIFO (…)` line to `test-fixtures/profiling/sample-export/expected.txt` after the coalescing-ratio line and one sentence to the fixture's header comment saying it is pre-#45 shaped (reads on the NIO thread, chunks on the pump thread, one coalesced group) and is run with `--pairing global`; verify `diff <(scripts/profile-summary.sh --pairing global test-fixtures/profiling/sample-export) test-fixtures/profiling/sample-export/expected.txt` fails against the unchanged script (unknown option)

## 2. Script (design D1, D2, D3, D5) — Green

- [x] 2.1 Argument parsing in the bash part: `--pairing per-thread|global` (default `per-thread`) before the positional input, usage line updated, unknown value or missing input → `die` with the usage; pass the mode to the python program as a third argument; verify `scripts/profile-summary.sh --pairing bogus test-fixtures/profiling/sample-export` exits 1 with one line and no traceback, and `shellcheck scripts/profile-summary.sh` is clean
- [x] 2.2 Signpost parsing: add `thread` to the required columns, read the row's `<thread><tid>` text through `resolve`, `Fail` on a sentinel thread cell or a thread without `tid`, and carry the tid in the event tuple; verify `scripts/profile-summary.sh --pairing global test-fixtures/profiling/sample-export` still matches its `expected.txt` except for the not-yet-printed `pairing:` line
- [x] 2.3 Pairing pass: print the `pairing:` line; implement the per-thread one-pending-read rule of D1 (chunks without a read, reads without a chunk, byte-mismatch errors consuming both events and printed with the D1 message and the `thread_label()` form) and keep the existing byte-sum FIFO for `global`; print `reads per chunk` only in global mode; latency suffix per D3 for each mode; `n/a` lines unchanged when there are no `TransportRead` events; verify the three `diff` commands from 1.2, 1.3 and 1.4 are empty, `bash -n` parses, `shellcheck` is clean, and running each fixture twice gives identical output
- [x] 2.4 Edge paths: verify `--pairing global` on `per-thread-export` prints the overshoot/underflow pair and the `reads per chunk` line (the numbers stated in the fixture's header comment); verify a copy of the fixture with one signpost row's thread cell replaced by `<sentinel/>` exits 1 with a one-line message
- [x] 2.5 Update the script's header comment: Usage with the flag, the Conventions bullet on pairing rewritten for the two modes and the new counts; verify `SignpostContractTests` still passes (`swift test --disable-sandbox --filter SignpostContractTests`) since it scans the script for the five names

## 3. Real-bundle check (operator-local, design Context) — not required by the spec

These two tasks need the gitignored bundles in `../RandomPlayer/profiling/`. On a machine without them, tick the box with the note "not run: bundles absent" and say so in the PR; the numbers they check are already recorded in `docs/PROFILING.md` and were reproduced by the probe during planning.

- [x] 3.1 Run the script in default mode on `../RandomPlayer/profiling/rc5-run{1,2,3}.trace` and verify it prints 15141 / 19708 / 26494 pairs, 1 / 1 / 0 chunks without a read, 0 reads without a chunk, 0 pairing errors, 13 / 9 / 10 delivery threads, and the pump-hop percentiles already recorded in the After table (median 0.002 ms; p95 0.012 / 0.008 / 0.007 ms; max 5.551 / 0.578 / 2.078 ms); save run 3's full output for task 4.1
- [x] 3.2 Run `--pairing global` on `../RandomPlayer/profiling/baseline-run2.trace` and verify the pump-hop line equals the recorded Baseline (6207 pairs, 0 pairing errors, 0.016 / 0.031 / 0.372 ms) and `reads per chunk` is printed, and save the full output for task 4.1; run default mode on the same bundle once and confirm the output is unmistakably wrong-mode (thousands of chunks without a read, reads without a chunk and mismatches), which is the D2 trade-off

## 4. Documentation (design D6)

- [x] 4.1 `docs/PROFILING.md`: Export and summarise (usage with the flag, the three fixtures, the three `diff` commands), Metrics rows for Coalescing ratio and Pump-hop latency (the three causes of a chunk without a read and their expected magnitudes), the Baseline (run 2, `--pairing global`) and After (run 3, default mode) script-output blocks replaced with the outputs saved in 3.1 / 3.2 and each marked as regenerated with the #69 script (every line outside the pairing block must be identical to the previous block — verify with `diff`), After table notes on the "Connections" and "per-thread pairing" rows and the "Global-FIFO pairing errors" row, the procedure note rewritten from "is a follow-up" to done (#69), Using the baseline step 2; verify the three `diff` commands in the doc run as written and are empty, and `swift test --disable-sandbox --filter SignpostContractTests` passes

## 5. Verification gate

- [x] 5.1 Run `/opsx:verify`; confirm every scenario of the delta spec maps to a fixture `diff` or an edge-path check in 2.4, that the artifacts describe the shipped flag and output shape, and that the dead-code gate holds (no new symbol without a call site; the only new files are fixtures); then `/opsx:archive`
