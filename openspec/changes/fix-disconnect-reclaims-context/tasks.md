## 1. Test observability (D-5)

- [x] 1.1 Added `SMB2Client.CBData.liveCount` (`AMSMB2/Context.swift`) — `NSLock`-guarded counter, `+1` in `init`, `-1` in `deinit`, with the implementation comment recording why it is unconditional rather than `#if DEBUG` (identical release/debug behaviour, no configuration-dependent `CBData` layout, matches the existing unconditional `pendingSeamOperationCount` / `hasInstalledSeamBridge` accessors) and why baseline-relative assertions are mandatory. Covered by `SMB2DisconnectReclaimTests.testCBDataLiveCountTracksInstances` (count rises by one and returns to baseline). The backing storage is `nonisolated(unsafe) private static var` with a comment stating the lock — not the compiler — is the safety argument; Swift 6 rejects a plain `static var` here.
- [x] 1.2 DECISION: moved `pendingSeamOperationCount` OUT of the `#if canImport(Network)` gate, keeping the name (it has existing seam test call sites) and updating its doc comment to say it is platform-neutral — the pending table backs both the seam and the legacy fd path. `hasInstalledSeamBridge` stays inside the gate (it reads `transportBridge`, which only exists on the seam). No new `pendingOperationCount` alias was added, so 2.1/2.3 compile and run unchanged under `make linuxtest`.

## 2. RED — regression tests that fail on current code

- [x] 2.1 `SMB2DisconnectReclaimTests.testDisconnectReleasesPendingCallbackObjectAndClient` in the NEW platform-neutral file `AMSMB2Tests/SMB2DisconnectReclaimTests.swift` (`import SMB2`, NOT inside `#if canImport(Network)`). `smb2_echo_async` DID queue on a never-connected context, as the review predicted — no fallback needed. Built with `SMB2Client(timeout: 0)` so no per-operation timer captures `cb` strongly (the timers are out of scope and would otherwise hold the emptied `CBData` shell past `disconnect()`), with a comment in the test saying so. RED evidence pre-fix: `XCTAssertEqual failed: ("1") is not equal to ("0") - disconnect() must destroy the context so libsmb2 balances every pending CBData retain` and `XCTAssertTrue failed - no pending operation may keep the client alive after disconnect()`. The `ENOTCONN` assertion already passed pre-fix (that part is unchanged behaviour). Post-review: the `liveCount` assertion is `XCTAssertLessThanOrEqual(…, baseline)` — the counter is process-global and other suites leave 30 s timers holding emptied shells, so it can legitimately fall below the baseline; the intent is "this test leaked nothing".
- [x] 2.2 `SMB2DisconnectReclaimTests.testSeamDisconnectReleasesPendingConnectAndBridge`, in a `#if canImport(Network)` extension at the bottom of the same new file. Uses `SMB2Client(timeout: 0)` (same timer reason as 2.1) and `TransportBridge(transport: MockTransport(sendsAreDropped: true))`, dropping the test's own strong bridge reference right after starting the connect task. RED evidence pre-fix: `XCTAssertEqual failed: ("2") is not equal to ("1") - disconnect() must destroy the context so the abandoned connect CBData is released` and `XCTAssertTrue failed - disconnect() must destroy the context so ext_close balances the ext.userdata retain on the bridge`. The `ENOTCONN`, `pendingSeamOperationCount == 0`, `hasInstalledSeamBridge == false` and weak-client assertions already held pre-fix (the connect path's `cb.cleanup` is `[weak self]`, so it never pinned the client — only the bridge and the `CBData` leaked). Post-review: same `XCTAssertLessThanOrEqual` comparator as 2.1; `weak let` for the client/bridge observers.
- [x] 2.3 `SMB2DisconnectReclaimTests.testDisconnectIsTerminalAndLaterOperationsFailFast`, built with `SMB2Client(timeout: 30)` (a positive timeout is what makes the fail-fast assertion meaningful; no `liveCount` assertion here). RED evidence pre-fix: `XCTAssertEqual failed: ("POSIXErrorCode(rawValue: 60)") is not equal to ("POSIXErrorCode(rawValue: 57)") - post-disconnect operations must fail with ENOTCONN` (ETIMEDOUT instead of ENOTCONN) and `XCTAssertLessThan failed: ("30.091199040412903") is not less than ("5.0") - post-disconnect operations must fail fast…` — i.e. the PDU queued into the still-live context and hung for the full 30 s timeout. `fileDescriptor == -1` / `isConnected == false` / the second `disconnect()` already held pre-fix. Post-review: the client is now held in a `var` and explicitly released (`client = nil`) with a `weak let` nil check, so the "releasing the client does not crash / single destroy" clause the task asks for is actually exercised.
- [x] 2.4 `SMB2DisconnectTimeoutTests.testNonGracefulDisconnectMidReadReleasesClient` (inherits `SMBIntegrationTestCase`, so it skips without `SMB_SERVER`). Writes 4 MiB, starts a read task whose progress callback fulfils a `readStarted` expectation, captures `weak var weakClient = try smb.smbClient` while the read is in flight, `disconnectShare(gracefully: false)`, awaits the read (success-if-completed-early or error both accepted), then polls `CBData.liveCount` back to baseline with an 8 s deadline (`smb.timeout = 3` bounds the per-operation timer that holds the emptied shell; restored to 60 in a teardown block) and polls `weakClient == nil` for up to 2 s. Sleep-based polling helper, no busy-spin. NOT RUN: no Docker host is available in this environment, so `make integrationtest` could not be executed — the test compiles (`swift build --disable-sandbox --build-tests` clean) and skips locally. Post-review: payload raised 4 MiB -> 32 MiB and the do/catch now records which branch ran, asserting `readFailed` — otherwise a read that finished before the disconnect landed would leave nothing pending and the test would pass against the unfixed code too. Shared `waitUntil(timeout:condition:)` now lives in `AMSMB2Tests/TestUtilities.swift` (not named `poll`, which would shadow C `poll(2)`). First real run (Docker): the progress-callback trigger was WRONG — the read loop pipelines 4 × `optimizedReadSize` (8 MiB against Samba) per window, so a 32 MiB file completes in ONE window and the first progress call fires after the whole file has arrived; `readFailed` was false. Trigger replaced by the exact in-flight signal: `waitUntil { client.pendingSeamOperationCount > 0 }` on the manager's client (strong ref scoped in a `do` block so it cannot pin the client through the release assertion). Passes against Samba on both transport legs.

## 3. GREEN — implementation in `AMSMB2/Context.swift`

- [x] 3.1 D-2: both `cb.dataHandler` wrappers (`async_await`, `async_await_pdu`) now capture `[weak self]` with `guard let self else { return }`, and carry a comment pointing at the ordering invariant (the handler runs only on `generic_handler`'s non-abandoned branch, and every deinit-driven destroy fails/abandons pending operations first).
- [x] 3.2 D-3: `generic_handler`'s `isAbandoned` early-return now sets `cbdata.cleanup = nil; cbdata.dataHandler = nil` before returning, with a comment explaining this is the last touch of an abandoned `CBData` and breaks the seam connect path's `cleanup -> cb` self-retain plus the caller's `dataHandler` captures.
- [x] 3.3 D-1: added `private func failPendingAndDestroyContext(with:)` containing exactly `failAllPendingOperations(with: error)` followed by the `if let`-guarded `destroyContext` + `context = nil`, with both required comments (fail-before-destroy is load-bearing for D-2's `[weak self]`; moving `shutdown()`'s fd-path disconnect PDU + `smb2_service(POLLOUT)` ahead of the fail step is safe because a suspended `async_await` caller retains `self`). Called from `shutdown()` with `ECANCELED` and from `disconnect()` with `ENOTCONN` on the seam arm, the fd arm, and the fd arm's early-exit branch, preserving PDU-flush -> transport teardown -> fail -> destroy. `deinit`'s `guard context != nil` is unchanged. Guard suites re-run green: `SMB2CBDataLifetimeTests|SMB2ServicingLoopTests|SMB2SeamConnectOrderingTests|BridgeOwnershipHandoffTests|SMB2CancelStressTests|GlobalContextListRaceTests` -> 28 executed, 1 skipped (integration-gated `testConcurrentReadCancellationDoesNotCrash`), 0 failures.
- [x] 3.4 D-4: doc comment added to `disconnect()` — terminal for the instance, no context afterwards, every later operation (including through a previously opened `SMB2FileHandle` / `SMB2Directory`) fails immediately with `ENOTCONN`, construct a fresh client to reconnect (`SMB2Manager.connect(shareName:)` already does), plus why destroying here is what balances the pending `CBData` retains and the seam bridge's `ext.userdata` retain, and that a repeat call is harmless.

## 4. Verification

- [x] 4.1 `swift test --disable-sandbox` (macOS): `Executed 291 tests, with 69 tests skipped and 0 failures` — every skip is server-gated (`SMB_SERVER`/`SMB_QUIC_SERVER`), no unit test skipped. ASan (`swift test --disable-sandbox --sanitize=address --filter 'SMB2DisconnectReclaimTests|SMB2CBDataLifetimeTests|SMB2ServicingLoopTests|BridgeOwnershipHandoffTests|GlobalContextListRaceTests|SMB2CancelStressTests'`, run twice — before and after the review fixes): `Executed 28 tests, with 1 test skipped and 0 failures`, no AddressSanitizer reports; the skip is the server-gated `SMB2CancelStressTests/testConcurrentReadCancellationDoesNotCrash`
- [x] 4.2 Integration suite GREEN on both transport legs against the `ghcr.io/simplekube-ro/samba:1.2` fixture (Colima): legacy `Executed 291 tests, with 22 tests skipped and 0 failures`; `SMB_TRANSPORT=seam` `Executed 291 tests, with 17 tests skipped and 0 failures` (skips are QUIC-gated). 2.4, 4.4a and all pre-existing `SMB2DisconnectTimeoutTests` passed; no `known-flaky` lane hits (the list is empty). Host caveat: macOS File Sharing (`com.apple.smbd`) owns port 445 on this machine and shadowed the compose port mapping (every test hit the Mac's own smbd → `STATUS_LOGON_FAILURE`), so the suite was run with the same container published on host port 4455 and the script's env (`SMB_SERVER=smb://127.0.0.1:4455`, `SMB_SHARE/USER/PASSWORD`) exported manually instead of via `make integrationtest`. First run exposed the 2.4 trigger bug recorded above; fixed and re-run green
- [x] 4.3 iOS build PASSED (`xcodebuild -scheme AMSMB2 -destination 'generic/platform=iOS' build` → `** BUILD SUCCEEDED **`). `make linuxtest` (swift:6.1 image) PASSED: `Executed 142 tests, with 53 tests skipped and 0 failures`; `SMB2DisconnectReclaimTests` (2.1, 2.3 and the liveCount foundation test) executed and passed on Linux, proving the fd-path `disconnect()` arm. First Linux run failed to compile because `weak let` requires Swift 6.2 — the five test sites were changed to `weak var` + separate assignment (accepted by 6.1, no never-mutated warning on 6.2)
- [x] 4.4a `SMB2ManagerTests.testConnectShareAfterListSharesUsesFreshClient` (integration-gated): `listShares()` → `connectShare` → `echo()` + `contentsOfDirectory("/")` must succeed on the rebuilt client. Reviewer confirmed `with(shareName:)` never calls `setClient(nil)` and `needsReconnect` now deterministically sees `fileDescriptor == -1`. Not executed locally (no Docker). Original task text: Regression guard for the reuse-audit gap: add/extend a test asserting that after `SMB2Manager.listShares()` (which routes through `with(shareName:)` at `AMSMB2.swift:1683` and leaves `self.client` pointing at the disconnected client without `setClient(nil)`), a following `connectShare(name:)` still succeeds and operations work — i.e. `needsReconnect` correctly rebuilds now that `fileDescriptor` falls back to `-1`. Integration-gated is fine
- [x] 4.4 Dead-code sweep: `CBData.liveCount` has 15 references in tests; `failPendingAndDestroyContext(with:)` has 4 call sites (`shutdown()` + all three `disconnect()` arms); `waitUntil(timeout:condition:)` has 6 test call sites; `shutdown()`/`disconnect()` contain no duplicate fail+destroy sequence. The three `smb2_service`-error paths still call `failAllPendingOperations` directly AFTER their own destroy (pre-existing destroy-then-fail order, explicitly out of scope per design Risks)
- [x] 4.5 Artifacts updated for the one deviation found: the per-operation timeout timers capture `cb` strongly, so `liveCount` returns to baseline immediately only when no timer is armed — recorded in design.md D-5 (with the `[weak cb]` follow-up) and in the two spec scenarios that previously said "before `disconnect()` returns"; unit tests use `timeout: 0`, the integration test polls. No 2.1 fallback was needed. Review pipeline: swift-code-reviewer NEEDS FIXES (10 findings) → all addressed → re-verified APPROVE (after reverting one stray 32 MiB hunk from `testShortTimeoutFiresOnLargeWrite`). `openspec validate fix-disconnect-reclaims-context --strict` passes
